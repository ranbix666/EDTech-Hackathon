{
  "name": "prober-ai-monorepo",
  "private": true,
  "workspaces": [
    "essay-tutor"
  ],
  "scripts": {
    "start": "npm --workspace essay-tutor start",
    "check": "npm --workspace essay-tutor run check",
    "test": "npm --workspace essay-tutor test",
    "export:study": "npm --workspace essay-tutor run export:study"
  },
  "engines": {
    "node": ">=20"
  }
}
